# SpendPilot Frontend Environment Variables

# The base URL of the deployed Express backend (appended with /api)
# In production, replace with your Render Web Service URL (e.g. https://spendpilot-backend.onrender.com/api)
VITE_API_URL=http://localhost:3001/api
